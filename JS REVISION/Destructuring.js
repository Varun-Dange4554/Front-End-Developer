// 📌 What is Destructuring? ->
//  Extract values from array or object
// Store them in separate variables easily

let obj = {
  name: "varun",
  age: 21,
};

let { name, age } = obj;
console.log("🚀 ~ age:", age);
console.log("🚀 ~ name:", name);

// # 1.de-structing : Exact properties from object easily
// # 2. we can extract more than one property at once
//  # 3.
